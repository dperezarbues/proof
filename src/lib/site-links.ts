// Single source of truth for the project's GitHub URL — used by the landing
// page's open-source section and by SiteFooter, so the two can't drift apart.
export const GITHUB_REPO = 'https://github.com/dperezarbues/proof'
